import React from "react";
import PackageSidebar from "../components/PackageSidebar";

const Package = () => {
  return (
    <div>
      <PackageSidebar />
    </div>
  );
};

export default Package;
