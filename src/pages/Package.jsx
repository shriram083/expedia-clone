import React from "react";
import PackageSidebar from "../components/PackageSidebar";
import TopInputBox from "../components/TopInputBox";

const Package = () => {
  return (
    <div>
      <TopInputBox />
      <PackageSidebar />
    </div>
  );
};

export default Package;
